<h1>Articles tagged with <%= @tag.name %></h1>

<ul id="tags">
	<% @tag.articles.each do |article| %>
	<li>
		<%= link_to article.title, article_path(article) %>
	</li>
	<% end %>
</ul>

<!-- add a link to back to same article -->